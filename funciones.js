document
  .getElementById("loginForm")
  .addEventListener("submit", function (event) {
    // El evento de enviar el formulario será manejado por el navegador automáticamente
    // Puedes agregar aquí lógica adicional si es necesario
  });
